id: T-394
status: partial
worker: claude-code
branch: agent/claude/T-394
summary: Source patch for EKA-14 and EKA-15 is committed (0156da4) on base 63788a1. EKA-14: the GitHub login/avatar sinks in docs/tickets/polis.js (renderAuthStatus, plus the comment-author login at the old line 430) and in docs/index.html (checkTicketAuth) are now built with createElement/textContent/.src, so no HTML is parsed. Avatars are accepted only as https://avatars.githubusercontent.com URLs; the size is set through URL.searchParams, and any other URL means no <img> is rendered. The polis.js GitHub error message and comment login now go through the existing escapeHtml. The qr-login.html API `detail` is rendered via textContent. A non-string detail (for example a FastAPI 422 array, which used to show "[object Object]") now falls back to "API Error <status>". Constant bilingual/static markup (the logged-out login button, skeletons, the empty state, the qr-container reset) is left unchanged. EKA-15: removed `adminApi` (every admin_key query/body constructor) and `_post`, a non-exported helper whose only callers were in `adminApi`. Status is partial only because runtime validation could not be run here (see tests).
files: docs/tickets/polis.js, docs/index.html, docs/embed/qr-login.html, apps/web/src/lib/api.ts, scripts/static-docs-remote-sinks.check.mjs (new), .github/workflows/ci.yml (Web job also runs the new check — additive only), scripts/redesign/r2_landing_check.py (reviewed fingerprint for landing inline script #5 → 25de9491…/21969 bytes), scripts/redesign/approved_analytics_delta.py (new, separately named SECURITY_REMEDIATION_SHA256 pinning docs/embed/qr-login.html to 342b7108…), scripts/redesign/test_r0_inventory.py (normalizes that pinned page), scripts/redesign/test_approved_analytics_delta.py (asserts the pin covers only qr-login.html)
tests: No OS-enforced isolated sandbox was available in this worktree (no node_modules and no sandbox runtime), so no target-controlled tests or builds were executed. What was actually run: (1) `node --check` on docs/tickets/polis.js and scripts/static-docs-remote-sinks.check.mjs, plus on every inline <script> block extracted from docs/index.html (7) and docs/embed/qr-login.html (2) → all parse OK. (2) `python3 -m py_compile` on the 4 changed redesign scripts → OK. (3) The landing inline-script fingerprints were recomputed with my own html.parser replica of the r0_inventory script extraction. The replica reproduced the pinned HEAD hashes 034faa7e… and 0ac3cf84… exactly, and only block #5 changed. The landing interaction/storage/API contracts (addEventListener 3, setInterval 3, setTimeout 9, sessionStorage 6, fetch( 8, URL/API-path/fetch-arg sets) are identical before and after. (4) Reference search: `git grep adminApi` finds only the audit doc, and `git grep admin_key -- apps/web` finds 0 hits; `_post` had no callers outside adminApi. needs_validation: `node --test scripts/static-docs-remote-sinks.check.mjs`, whose 10 tests cover: hostile login in polis.js and index.html rendered as text only; 8 hostile avatar URLs (javascript:, data:, http:, foreign host, look-alike host, attribute breakout, empty, null) dropped; normal avatar kept with &s=56 / &s=40; escaped polis error; hostile and non-string qr-login detail; no adminApi/admin_key in apps/web/src. Also needs_validation: `cd apps/web && npm ci && npm run lint && npm run typecheck && npm test && npm run build`; `python -m unittest discover scripts/redesign` (r0 inventory, r2/r3/r4 gates, approved delta); a browser check of the tickets page auth chip, the landing ticket tab and the qr-login error state.
risks: (a) I wrote the new node:test regression but never ran it, so a harness bug in its fake DOM or vm extraction could show up as a CI failure rather than a product bug. (b) The redesign gate hashes were computed with a replica of the parser, not with r0_inventory itself. The replica matched the existing pins, but the gate still has to be run. (c) Behavior changes: avatars not hosted on avatars.githubusercontent.com (for example GitHub Enterprise) are no longer shown; a non-string qr `detail` now shows "API Error <status>"; the landing logout button gets type="button". (d) The landing checkTicketAuth sink is currently unreachable because #ticketLoginPrompt and #ticketUserInfo are missing from docs/index.html markup, so that fix is defense-in-depth. (e) Out of scope and still present: `issue.html_url` interpolated into href in polis.js renderDetail and in the index.html ltSuccess block; `params.purpose` is not URL-encoded in qr-login.html; category/status values in the polis.js templates come from constant config.
security: Threat model: an attacker controls GitHub-sourced strings (login, avatar_url, API error text) or the API `detail` and tries to get markup or script executed in the ekklesia.gr docs origin. That origin holds the polis_token in sessionStorage, and per EKA-07 the Ed25519 identity key is in the same origin's storage. Mitigations: remote text never reaches an HTML parser (textContent), or is escaped when it has to join existing template HTML (escapeHtml, used only in element-content context). The img src must parse as an absolute https URL on the exact host avatars.githubusercontent.com, which blocks javascript:, data: and attribute-breakout values. Handlers are function-valued, never strings. No new attack surface, no auth/API change, no dependency change, no secret or query logging. EKA-15 removes a shipped client pattern that put admin keys in query strings; the API was already Bearer-only.
next: Codex: review the diff; run the needs_validation commands in an isolated environment or in CI; do a browser check of the tickets auth chip, the landing ticket tab and the qr-login error. Decide whether the out-of-scope `issue.html_url` href sinks need a follow-up ticket. Do not close #318 or EKA-14/15 until review, CI and the live-adoption gate pass. No push, PR, merge or deploy was done.
