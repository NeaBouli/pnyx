id: T-397
status: partial
worker: claude-code
branch: agent/claude/T-397
summary: T-394 was cherry-picked cleanly onto origin/main 63788a1034a1 (0156da4 became e52fd3a, 18eafd1 became ad15048), and every automated gate is green. Exactly one validation fix was needed and committed as 04d538a. The T-394 regression test `polis.js: comment author login is escaped` failed (9/10) because its regex `\+\s*polisUser\.(login|avatar_url)\s*\+` also matched docs/tickets/polis.js:309. That line is the GitHub claim-comment body `JSON.stringify({ body: "🙋 **Claimed by @" + polisUser.login + ...})`, a markdown API payload rather than an HTML sink, and it is unchanged since main. This is the harness bug that T-394 predicted as risk (a). The fix excludes only `JSON.stringify(` lines from that one assertion. Negative control: the narrowed assertion still flags the original main renderAuthStatus sink. No product code was changed. In real Chromium and WebKit, the branch renders all hostile remote text inertly; the base tree shows that the XSS was real. Status is partial, not ok, because of one exact open acceptance blocker: the Landing ticket-tab/auth state is unreachable (see risks). Two pre-existing layout defects were also found; both are identical on base and are not T-394 regressions.
files: scripts/static-docs-remote-sinks.check.mjs (validation fix, +3/-1, commit 04d538a); .fleet/reports/T-397.md (this report). T-394 scope, unchanged from the reviewed commits: .fleet/reports/T-394.md, .github/workflows/ci.yml, apps/web/src/lib/api.ts, docs/embed/qr-login.html, docs/index.html, docs/tickets/polis.js, scripts/redesign/approved_analytics_delta.py, scripts/redesign/r2_landing_check.py, scripts/redesign/test_approved_analytics_delta.py, scripts/redesign/test_r0_inventory.py, scripts/static-docs-remote-sinks.check.mjs. Commits on top of 63788a1: e52fd3a, ad15048, 04d538a, plus the report commit.
tests: (1) Scope: `git diff --stat 63788a1..HEAD` shows the 11 expected T-394 paths; the only extra change is the 3-line hunk in the check file above. `git diff --check 63788a1..HEAD` is clean. (2) `node --test scripts/static-docs-remote-sinks.check.mjs`: before the fix, tests 10, pass 9, fail 1; after the fix, tests 10, pass 10, fail 0. (3) apps/web, following the CI Web verify chain in .github/workflows/ci.yml:95. `npm ci --ignore-scripts` found 0 vulnerabilities. With CI's Node from .nvmrc (22.13.0; official nodejs.org darwin-x64 tarball in /tmp, SHA256 verified against SHASUMS256.txt): `node --test ../../scripts/client-yaml-security.check.cjs image-codec.check.mjs ../../scripts/static-docs-remote-sinks.check.mjs` gave 22/22 pass; `npm run lint` exit 0; `npm run typecheck` exit 0; `npm test` exit 0 with 7 files and 77 tests passed; `npm run build` exit 0 ("Compiled successfully", 3/3 static pages). Note: under the host's default Node 22.11.0, `npm test` exits 1 (6 files and 48 tests pass, but src/app/[locale]/sso-verify/page.test.tsx cannot start its worker: ERR_REQUIRE_ESM for @exodus/bytes via html-encoding-sniffer/jsdom). That is a host Node-version limitation, unrelated to T-394; CI pins 22.13.0. (4) Redesign gate: `python3 -m unittest discover -s scripts/redesign -p 'test_*.py'` (the command in docs/planning/r3/R3_REPORT.md) ran 254 tests, OK (Python 3.11.5). The CLI gates also pass on the branch: r1_foundation_check, r2_landing_check, r3_wiki_pilot_check and r4_community_header_check all exit 0. Caveat: running `python3 scripts/redesign/r0_inventory.py` rewrites the tracked docs/planning/r0/R0_DOCS_SURFACE_INVENTORY.json and R0_REPORT.md, and r2/r3/r4 then fail on stylesheet order. I restored both files with git checkout and reran; all three pass, and they also pass on a git-archive copy of base 63788a1. (5) Browser: Playwright 1.63.0 on Chromium 153.0.8010.12 and WebKit 26.6, viewports 1440x900 and 390x844. The branch docs/ tree was served on 127.0.0.1:8397 and a git-archive of base 63788a1 on :8398 for comparison. Every non-localhost request was intercepted: api.github.com and api.ekklesia.gr were stubbed, avatars.githubusercontent.com got a 1x1 PNG, everything else got 204. The token was a fake string ("stub-token-not-real"); no real OAuth, identity or network. The hostile string was `<img src=x onerror="window.__pwned=1">"><svg onload=window.__pwned=2>`. Branch results, identical on all 4 engine/viewport combinations unless noted: (a) Tickets auth chip, hostile login plus valid avatar: login span textContent equals the hostile string, 0 injected img[src=x]/svg[onload], window.__pwned unset; avatar src https://avatars.githubusercontent.com/u/1?v=4&s=56 loaded; logout button visible, and clicking it removes polis_token, removes the chip and shows .btn-login. (b) avatar_url `javascript:...`: no img rendered; chip and logout work. (c) Attribute-breakout avatar on the allowlisted host: the quote becomes percent-encoded inside src, no attribute or handler is injected, and the value is inert. (d) Ticket-list error: a 422 with a hostile GitHub `message` renders as text inside .empty-state p, and the retry button is present. (e) Comment author login (detail activity feed): at 1440 the hostile text renders as text and is visible, 0 injected. At 390 it is also rendered as text with 0 injected, but not visible because of the pre-existing defect in risks. (f) QR error: hostile 400 `detail` renders as literal text, color rgb(220,38,38), pending state hidden. Clicking the refresh button re-issues exactly 1 qr-session request and re-renders inertly. A non-string 422 detail shows "API Error 422" (base showed "[object Object]"). (g) Console and page errors: 0 pageerror, 0 dialogs, 0 non-network console errors in every scenario. The only console errors are "Failed to load resource" from the deliberately stubbed 204/422/400 responses. (h) Overflow: scrollWidth equals clientWidth at 1440 everywhere, and at 390 for the QR, ticket-error, logged-out tickets and landing pages. Base comparison: every hostile scenario on base 63788a1 gives window.__pwned=1 and 2 injected nodes, which confirms the fix is effective. 54 screenshots are in /tmp/t397_browser/shots and the harness is at /tmp/t397_browser/{run,extra,vis}.cjs; neither is committed. Not verified: a live GitHub OAuth round trip, the production CSP/headers of the deployed origin, GitHub CI/Security Audit/CodeRabbit, and the Landing ticket state (blocker below).
risks: OPEN ACCEPTANCE BLOCKER: the Landing ticket-tab/auth state cannot be reached. On both base and branch, docs/index.html has no element with id tabTicket, tabContact, ticketMode, contactMode, ticketLoginPrompt, ticketUserInfo or ticketFormLanding (0 matches). Calling checkTicketAuth() with a token throws "Cannot read properties of null (reading 'style')" in Chromium and "null is not an object (evaluating 'document.getElementById("ticketLoginPrompt").style')" in WebKit. The landing page itself loads with no page errors and no overflow at either width, in both engines. The T-394 checkTicketAuth patch is therefore defense-in-depth that was verified only by the node:test fake DOM, not in a browser. Restoring the markup is a UX/product decision and is out of scope. Pre-existing, identical on base, not T-394: (1) at 390px the logged-in tickets top bar overflows horizontally. With a realistic 39-character GitHub login, scrollWidth/clientWidth is 552/390 on both base and branch in Chromium and 558/390 on both in WebKit; the overflowing elements are .polis-actions, #authStatus .auth-status and #qrLoginBtn. Logged out there is no overflow. (2) On viewports ≤1024px the mobile ticket-detail modal opens empty: renderMobileDetail moves #detailPanel into #mobileDetailBody, but docs/tickets/style.css:100 `@media(max-width:1024px){ .polis-detail { display:none } }` still applies, so the content has a 0x0 rect (reproduced on base and branch). I left both unfixed as out-of-scope UX/layout work; they need a follow-up ticket. Residuals from T-394, still out of scope: `issue.html_url` in href (polis.js renderDetail and index.html ltSuccess), `params.purpose` not URL-encoded in qr-login.html, generic innerHTML templates built from constant config. Changed behavior: non-avatars.githubusercontent.com avatars are hidden, a non-string QR detail shows "API Error <status>", and the landing logout button has type=button.
security: Threat model (unchanged from T-394): GitHub-sourced strings (login, avatar_url, error message, comment author) or the QR API `detail` try to reach an HTML parser in the ekklesia.gr docs origin, which holds polis_token and the Ed25519 identity key in storage. The browser evidence shows that base 63788a1 executes injected handlers (window.__pwned=1) and that the branch neutralizes every tested vector in Chromium and WebKit. The validation fix narrows a source-regex assertion only for JSON API payload lines. It does not weaken the HTML-sink check, and the negative control confirms it still catches the original sink. No CSP, auth, URL-validation, CI, dependency or secret change. No real tokens were used, and there were no network egress or production writes during browser checks.
next: Codex: review 04d538a (the test-only fix) and accept. Push agent/claude/T-397 and open the PR using .fleet/T-394_PR_PREP.md, updating the "Required before merge" section with the results above. Wait for GitHub CI, Security Audit and CodeRabbit. Decide whether to file follow-ups for (a) the missing Landing ticket-tab markup (the acceptance blocker), (b) the 390px logged-in tickets top-bar overflow, (c) the empty mobile detail modal at ≤1024px, and (d) the remaining href/purpose sinks. Do not close #318 or EKA-14/15 before merge and live verification. No push, PR, merge or deploy was done.
